# Export post from Invision Power Board (IPB) to Movable Type =

Tested on IPB 2.1.7 and Movable Type 4.34.

## How to use this

1. Change settings in 'ipb-to-mt.php'.
2. Upload 'ipb-to-mt.php' to your IPB directory.
3. Run 'ipb-to-mt.php' in browser.
4. Save results as text file (UTF-8 encoding and Unix line breaks).
